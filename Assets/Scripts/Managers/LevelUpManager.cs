// Leveling no longer opens a card picker.
// Brother levels grant a passive stat bump + heal in Character.LevelUp().
// In-run upgrade picks now happen via RunBoonManager (see Boons system).
// Kept as a stub so the existing scene GameObject doesn't lose its component.
public class LevelUpManager : Singleton<LevelUpManager>
{
}
